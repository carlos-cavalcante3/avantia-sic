name: Pipeline Diária ETL Avantia

on:
  schedule:
    # 09:00 UTC corresponde às 06:00 no horário de Brasília (UTC-3)
    - cron: '0 9 * * *'
  workflow_dispatch: # Permite rodar manualmente pelo site do GitHub para testar

jobs:
  run-etl:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout do código
        uses: actions/checkout@v5

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar dependências
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Executar Pipeline ETL
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          RD_CLIENT_ID: ${{ secrets.RD_CLIENT_ID }}
          RD_CLIENT_SECRET: ${{ secrets.RD_CLIENT_SECRET }}
        run: python run.py
